http:
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true
    only-allow-nag:
      ipAllowList:
        sourceRange:
          - "10.10.12.0/24"
